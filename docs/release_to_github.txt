# Create release
git tag v0.1.0
git push origin v0.1.0

# Upload binaries to GitHub Release
gh release create v0.1.0 \
    dist/letta-acp-bridge-* \
    --title "v0.1.0 - Initial Release" \
    --notes "First working version of Letta ACP Bridge"